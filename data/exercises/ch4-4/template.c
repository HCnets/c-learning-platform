/*
 * 求 1 + 2 + ... + n 的和
 * 输入：一个整数 n（1 <= n <= 1000）
 * 输出：累加和
 * 例：输入 10，输出 55
 */
#include <stdio.h>

int main(void)
{
    int n, i, sum = 0;

    scanf("%d", &n);

    // TODO: 用 while / for 循环累加 1 到 n

    return 0;
}
