// 排序 —— vector + sort（竞赛最常用组合）
// 输入：第一行 n，第二行 n 个整数
// 输出：升序排序，空格分隔
// 例：输入 3 / 3 1 2，输出 1 2 3
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];

    // TODO: 用 sort 排序并输出（空格分隔，末尾换行）

    return 0;
}
