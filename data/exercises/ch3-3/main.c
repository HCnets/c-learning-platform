/*
 * 成绩分级
 * 输入：一个整数成绩 s（0~100）
 * 输出：A（s>=90）、B（s>=80）、C（s>=70）、D（s>=60）、E（其余）
 * 例：输入 95，输出 A
 */
#include <stdio.h>

int main(void)
{
    int s;

    scanf("%d", &s);

    // TODO: 用 if / else if 分级并输出

    return 0;
}
