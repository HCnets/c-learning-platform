/*
 * 统计字符串长度（自己实现 strlen）
 * 输入：一行字符串（不含空格，长度 <= 99）
 * 输出：字符串的长度
 * 例：输入 hello，输出 5
 */
#include <stdio.h>

int my_strlen(char s[])
{
    // TODO: 用循环数出字符个数，直到 '\0' 为止

}

int main(void)
{
    char s[100];

    scanf("%s", s);
    printf("%d\n", my_strlen(s));
    return 0;
}
